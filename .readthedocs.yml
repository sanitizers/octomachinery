version: 2

formats: all

build:
  image: latest

python:
  version: 3.7
  install:
  - requirements: .docs-requirements.txt
  #- method: pip
  #  path: pip >= 19.0.3
  - method: pip
    path: .
    extra_requirements:
    - docs
